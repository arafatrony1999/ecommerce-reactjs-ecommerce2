import React from 'react'

const ORDER_TITLE = (props) => {
    return (
        <div className="order-title w-100 px-3">
            {props.orderTitle}
        </div>
    )
}

export default ORDER_TITLE
import React from 'react';

const PROFILEBREAD = (props) => {
    return (
        <>
            <h5 className='profile-beard'>{props.pageTitle}</h5>
        </>
    );
}

export default PROFILEBREAD;
